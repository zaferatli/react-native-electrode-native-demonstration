import {FreeTrail} from './freeTrail.component';
import {
  FreeTrailDefaultPropsType,
  freeTrailDefaultProps,
} from './freeTrail.default';
import {FreeTrailStyle} from './freeTrail.style';
import {FreeTrailPropsType} from './freeTrail.type';

FreeTrail.defaultProps = freeTrailDefaultProps;

export default FreeTrail;
export {
  type FreeTrailPropsType,
  type FreeTrailDefaultPropsType,
  freeTrailDefaultProps,
  FreeTrailStyle,
};
