import {projectColors} from 'helper/stylesHelper';
import {StyleSheet} from 'react-native';

export const mainPageLayoutStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: projectColors.white,
  },
});
